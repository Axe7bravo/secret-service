import { useEffect, useState } from 'react'; import { NavLink, Outlet, useLocation } from 'react-router-dom'; import { PUBLIC_NAVIGATION } from '@secret-service/config'; import { CustomCursor } from './CustomCursor';
export function SiteLayout(){const [open,setOpen]=useState(false);const location=useLocation();useEffect(()=>setOpen(false),[location.pathname]);
 return <><CustomCursor/><header><div className="nav-container"><NavLink to="/" className="logo"><span className="logo-icon">S</span> SECRET SERVICE <span className="logo-plaque">EST. 1952</span></NavLink><button className="burger-menu" aria-label="Toggle Navigation" aria-expanded={open} onClick={()=>setOpen(v=>!v)}><span className="burger-line"/><span className="burger-line"/><span className="burger-line"/></button><ul className={`nav-menu${open?' active':''}`}>{PUBLIC_NAVIGATION.map(item=><li key={item.to}><NavLink to={item.to} className={({isActive})=>`nav-link${isActive?' active':''}`}>{item.label}</NavLink></li>)}<li className="nav-auth-item"><NavLink to="/login" className={({isActive})=>`nav-link nav-login${isActive?' active':''}`}>Log in</NavLink></li><li className="nav-auth-item"><NavLink to="/signup" className={({isActive})=>`nav-link nav-signup${isActive?' active':''}`}>Sign up</NavLink></li></ul></div></header><Outlet/><footer><div className="footer-container"><div><p>&copy; 2026 SECRET SERVICE AGENCY. ALL RIGHTS RESERVED.</p></div><div className="footer-sys-info"><span>LOC // 33.9249&deg; S, 18.4241&deg; E</span><span>STATUS: <span className="footer-status">ONLINE</span></span><NavLink to="/contact" className="footer-link">TERMINAL</NavLink></div></div></footer></>}
