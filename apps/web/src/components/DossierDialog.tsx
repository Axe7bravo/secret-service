import { useEffect, useRef } from 'react'; import type { Dossier } from '@secret-service/types'; import { formatPrice } from '../data/dossiers';
export function DossierDialog({dossier,onClose,onConfirm}:{dossier:Dossier|null;onClose:()=>void;onConfirm:(d:Dossier)=>void}){const close=useRef<HTMLButtonElement>(null);useEffect(()=>{if(!dossier)return;const previous=document.activeElement as HTMLElement|null;document.body.style.overflow='hidden';close.current?.focus();const key=(e:KeyboardEvent)=>{if(e.key==='Escape')onClose()};document.addEventListener('keydown',key);return()=>{document.body.style.overflow='';document.removeEventListener('keydown',key);previous?.focus()}},[dossier,onClose]);if(!dossier)return null;return <div className="modal-overlay active" role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="modal-content" role="dialog" aria-modal="true" aria-labelledby="dossier-title"><div className="modal-header"><h2 className="modal-title" id="dossier-title">{dossier.title}</h2><button ref={close} className="modal-close" aria-label="Close Modal" onClick={onClose}>&times;</button></div><div className="modal-body"><div className="modal-meta-grid"><div className="modal-meta-item"><span>Operational Cost</span><p className="price">{formatPrice(dossier.price)}</p></div><div className="modal-meta-item"><span>Staging Period</span><p>{dossier.timeframe}</p></div><div className="modal-meta-item"><span>Clearance Required</span><p>{dossier.clearance}</p></div><div className="modal-meta-item"><span>Operational Status</span><p style={{color:'var(--accent)'}}>READY TO DEPLOY</p></div></div><p className="modal-desc">{dossier.description}</p><div className="modal-actions"><button className="btn" onClick={()=>onConfirm(dossier)}>[ Initiate Comms ]</button></div></div></div></div>}
