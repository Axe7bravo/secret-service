import { useRef } from 'react'; import { usePageAnimations } from '../hooks/usePageAnimations';
export function ProtocolPage(){const scope=useRef<HTMLElement>(null);usePageAnimations(scope);return <main ref={scope}><section className="protocol-hero"><div className="section-container"><span className="section-subtitle">STANDARD OPERATING PROCEDURE</span><h1 className="hero-title page-hero-title with-margin">Operational Protocol</h1><p className="page-intro">Every dispatch moves through a rigid three-stage operational sequence, engineered for timing, theatre, and total discretion.</p></div></section><Protocol step="PROTOCOL_01 //" title="Directive Received" image="/assets/operational_protocol.jpg"><p>Your encrypted terminal request is logged, verified, and assigned a classified hash ID within our local ledger.</p><p>We automatically isolate billing details and discard client personal markers. The transaction leaves zero digital footprint on public infrastructure, ensuring absolute sender immunity.</p></Protocol><Protocol step="PROTOCOL_02 //" title="Covert Assembly" image="/assets/agent_hq_vibe.jpg"><p>Operatives assemble payloads, conduct site audits or target recon, and prepare physical hand-off folders.</p><p>Reconnaissance variables include tracking building access schedules, timing pedestrian flow, and mapping emergency escape corridors to guarantee perfect timing for the delivery window.</p></Protocol><Protocol step="PROTOCOL_03 //" title="Discretionary Dispatch" image="/assets/operational_protocol.jpg" dark><p>Tactical agents carry out the target handoff at the exact coordinates, leaving zero traceable links back to you.</p><p>No explanations are offered, no client names are muttered. Once the target receives the physical dispatch, the agent leaves the area immediately, completing the loop.</p></Protocol></main>}
function Protocol({step,title,image,dark,children}:{step:string;title:string;image:string;dark?:boolean;children:React.ReactNode}){return <section className="protocol-section"><div className="protocol-panel-content"><span className="protocol-step-num">{step}</span><h2 className="protocol-step-title">{title}</h2><div className="protocol-step-body">{children}</div></div><div className={`protocol-panel-visual${dark?' protocol-panel-visual-dark':''}`} style={{backgroundImage:`url('${image}')`}}/></section>}
