import { useRef } from 'react'; import { Link } from 'react-router-dom'; import { DossierCard } from '../components/DossierCard'; import { dossiers } from '../data/dossiers'; import { usePageAnimations } from '../hooks/usePageAnimations';
export function HomePage(){const scope=useRef<HTMLElement>(null);usePageAnimations(scope);return <main ref={scope}><section className="hero"><div className="hero-background"/><div className="hero-container"><div className="hero-content"><div className="hero-telemetry">SYSTEM_STATUS: ACTIVE // COMMS_SECURE</div><h1 className="hero-title">Delivered<br/>in Confidence.</h1><p className="hero-subtitle">Experiential dispatches, secret admirer missives, and covert pranks executed with military precision. Absolute anonymity guaranteed.</p><Link to="/contact" className="btn btn-glowing">[ Initiate Dispatch ]</Link></div></div></section><section className="active-operations"><div className="section-container"><div className="section-header"><span className="section-subtitle">CURRENT PORTFOLIO</span><h2 className="section-title">Active Operations</h2></div><div className="operations-grid">{dossiers.slice(0,3).map(d=><DossierCard key={d.id} dossier={d} preview/>)}</div></div></section><section className="protocol-overview"><div className="section-container"><div className="section-header"><span className="section-subtitle">STANDARD COMPLIANCE</span><h2 className="section-title">Operational Protocol</h2></div><div className="timeline"><Timeline number="01" title="Directive Received">Your encrypted terminal request is logged, verified, and assigned a classified hash ID within our local ledger.</Timeline><Timeline number="02" title="Covert Assembly">Operatives assemble payloads, conduct site audits or target recon, and prepare physical hand-off folders.</Timeline><Timeline number="03" title="Discretionary Dispatch">Tactical agents carry out the target handoff at the exact coordinates, leaving zero traceable links back to you.</Timeline></div></div></section></main>}
function Timeline({number,title,children}:{number:string;title:string;children:string}){return <div className="timeline-step"><div className="timeline-node">{number}</div><h3 className="timeline-step-title">{title}</h3><p className="timeline-step-desc">{children}</p></div>}
