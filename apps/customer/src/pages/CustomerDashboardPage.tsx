import { Link } from 'react-router-dom';import { CustomerOperationCard } from '../components/CustomerOperationCard';import { customerOperationsRepository } from '../data/customerOperationsRepository';import { isCompletedStatus } from '../utils/status';
export function CustomerDashboardPage(){const operations=customerOperationsRepository.list();const active=operations.filter(item=>!isCompletedStatus(item.status));const completed=operations.filter(item=>isCompletedStatus(item.status));return <main className="customer-page"><header className="customer-page-header"><div><p className="customer-eyebrow">PRIVATE OVERVIEW</p><h1>Your Operations</h1><p>Track every discreet detail from confirmation to completion.</p></div><Link className="customer-primary" to="/operations">View all operations</Link></header><section className="customer-metrics"><article><span>Active operations</span><strong>{active.length}</strong></article><article><span>Completed operations</span><strong>{completed.length}</strong></article><article><span>Total operations</span><strong>{operations.length}</strong></article></section><section><div className="customer-section-heading"><div><p className="customer-eyebrow">CURRENT FILES</p><h2>Recent Operations</h2></div><Link to="/operations">My Operations →</Link></div><div className="operation-card-grid">{operations.slice(0,3).map(item=><CustomerOperationCard key={item.operationId} operation={item}/>)}</div></section></main>}
