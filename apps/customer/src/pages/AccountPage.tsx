import {PageHeader} from '../components/PageHeader';import {SectionCard} from '../components/SectionCard';import {customerRepository} from '../data/customerRepository';import {formatDate} from '../utils/formatters';
export function AccountPage(){const customer=customerRepository.getCurrent();return <main className="client-main"><PageHeader eyebrow="CLIENT FILE" title="Account" description="Your private client identity and contact information." actions={<button className="prototype-edit" type="button" disabled title="Profile editing is not implemented">Edit Profile</button>}/><div className="account-grid"><SectionCard title="Profile" eyebrow="IDENTITY"><dl className="client-detail-list"><Detail label="First name" value={customer.firstName}/><Detail label="Last name" value={customer.lastName}/><Detail label="Client ID" value={customer.id}/><Detail label="Account created" value={formatDate(customer.createdAt.slice(0,10))}/></dl></SectionCard><SectionCard title="Contact Information" eyebrow="SECURE CONTACT"><dl className="client-detail-list"><Detail label="Email" value={customer.email}/><Detail label="Phone" value={customer.phone}/></dl><p className="account-note">Contact changes are disabled during this prototype milestone.</p></SectionCard></div></main>}
function Detail({label,value}:{label:string;value:string}){return <div><dt>{label}</dt><dd>{value}</dd></div>}
