import {type FormEvent,useState} from 'react';import {Navigate,useNavigate} from 'react-router-dom';import {customerAuthService} from '../auth/customerAuthService';import {MOCK_CUSTOMER_CREDENTIALS} from '../auth/mockCustomerAuth';
export function CustomerLoginPage(){const navigate=useNavigate();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');if(customerAuthService.isAuthenticated())return <Navigate to="/dashboard" replace/>;const submit=(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setError('');if(customerAuthService.login(email,password)){navigate('/dashboard',{replace:true});return}setError('Access denied. Check your credentials.')};return <main className="customer-login"><section aria-labelledby="customer-login-heading"><div className="login-brand"><span>S</span><div><strong>SECRET SERVICE</strong><small>PRIVATE CLIENT ACCESS</small></div></div><div className="login-classification"><span>CONFIDENTIAL FILES</span><span>CLIENT ACCESS ONLY</span></div><h1 id="customer-login-heading">Access Your Operations</h1><p>Authenticate to view your active and completed operations.</p><form onSubmit={submit}><div><label htmlFor="customer-email">Email</label><input id="customer-email" type="email" value={email} onChange={event=>setEmail(event.target.value)} autoComplete="username" required autoFocus/></div><div><label htmlFor="customer-password">Password</label><input id="customer-password" type="password" value={password} onChange={event=>setPassword(event.target.value)} autoComplete="current-password" required/></div>{error&&<p className="login-error" role="alert">{error}</p>}<button type="submit">Access Files</button></form><aside className="customer-dev-access"><span>DEVELOPMENT ACCESS · MOCK ONLY</span><dl><div><dt>Email</dt><dd>{MOCK_CUSTOMER_CREDENTIALS.email}</dd></div><div><dt>Password</dt><dd>{MOCK_CUSTOMER_CREDENTIALS.password}</dd></div></dl><p>Temporary frontend credentials. This is not secure authentication.</p></aside></section></main>}
