import { useCustomerAuth } from '../auth/customerAuthContext';
import { CustomerPageHeader } from '../components/CustomerPageHeader';
import { CustomerSection } from '../components/CustomerSection';
export function CustomerAccountPage(){const {user}=useCustomerAuth();return <main className="client-main"><CustomerPageHeader eyebrow="CLIENT FILE" title="Account" description="Your private client identity and secure contact information." /><div className="account-grid"><CustomerSection title="Profile" eyebrow="IDENTITY"><dl className="client-detail-list"><div><dt>Name</dt><dd>{user?.displayName||'Not supplied'}</dd></div><div><dt>Account type</dt><dd>Private Client</dd></div></dl></CustomerSection><CustomerSection title="Contact Information" eyebrow="SECURE CONTACT"><dl className="client-detail-list"><div><dt>Email</dt><dd>{user?.email||'Not supplied'}</dd></div><div><dt>Email verification</dt><dd>{user?.emailVerified?'Verified':'Not yet verified'}</dd></div></dl><p className="account-note">Identity details are controlled by Firebase Authentication. Editing remains unavailable until a reauthentication-safe profile flow is introduced.</p></CustomerSection></div></main>}
