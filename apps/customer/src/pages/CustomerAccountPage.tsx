import { useCustomerAuth } from '../auth/customerAuthContext';import { customerProfileRepository } from '../data/customerProfileRepository';
export function CustomerAccountPage(){const {user}=useCustomerAuth();const profile=customerProfileRepository.get();return <main className="customer-page"><header className="customer-page-header"><div><p className="customer-eyebrow">PRIVATE PROFILE</p><h1>Account</h1><p>Your read-only customer profile summary.</p></div></header><section className="customer-panel account-panel"><dl><div><dt>Name</dt><dd>{user?.displayName||`${profile.firstName} ${profile.lastName}`}</dd></div><div><dt>Firebase email</dt><dd>{user?.email||profile.email}</dd></div><div><dt>Phone</dt><dd>{profile.phone}</dd></div><div><dt>Member since</dt><dd>{new Date(profile.memberSince).toLocaleDateString('en-ZA')}</dd></div><div><dt>Email verification</dt><dd>{user?.emailVerified?'Verified':'Not yet verified'}</dd></div></dl><p className="account-note">Authentication is live through Firebase. Additional profile persistence will be introduced with the Firestore profile milestone.</p></section></main>}
