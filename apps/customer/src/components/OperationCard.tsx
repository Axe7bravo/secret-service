import { Link } from 'react-router-dom';import type { CustomerOperation } from '../types/customer';import { formatCurrency,formatDate } from '../utils/formatters';import { CustomerStatusBadge } from './CustomerStatusBadge';
export function OperationCard({operation}:{operation:CustomerOperation}){return <article className="client-operation-card"><header><div><span>{operation.operationId}</span><h2>{operation.packageType}</h2></div><CustomerStatusBadge status={operation.status}/></header><dl><div><dt>Recipient</dt><dd>{operation.recipientName}</dd></div><div><dt>Requested delivery</dt><dd>{formatDate(operation.requestedDeliveryDate)}</dd></div><div><dt>Amount</dt><dd>{formatCurrency(operation.amount)}</dd></div></dl><Link to={`/operations/${operation.operationId}`}>Open operation file <span aria-hidden="true">→</span></Link></article>}
