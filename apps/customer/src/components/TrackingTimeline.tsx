import type { CustomerOperationStatus } from '../types/customer';import { getCustomerStatus,trackingStages } from '../utils/status';
export function TrackingTimeline({status}:{status:CustomerOperationStatus}){const current=getCustomerStatus(status);const index=trackingStages.findIndex(item=>item.key===current.key);if(index<0)return <div className={`tracking-exception tracking-exception--${current.key}`} role="status"><strong>{current.label}</strong><span>{current.description}</span></div>;return <ol className="tracking-timeline">{trackingStages.map((stage,position)=><li key={stage.key} className={position<index?'complete':position===index?'current':'pending'}><span aria-hidden="true">{position<index?'✓':position+1}</span><div><strong>{stage.label}</strong>{position===index&&<small>Current stage</small>}</div></li>)}</ol>}
