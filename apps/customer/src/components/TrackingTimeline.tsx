import type { CustomerOperationStatus } from '../types/customer';import { getCustomerStatus,trackingStages } from '../utils/status';
export function TrackingTimeline({status}:{status:CustomerOperationStatus}){const current=getCustomerStatus(status);const index=trackingStages.findIndex(item=>item.key===current.key);const exceptional=current.key==='attention';return <ol className="tracking-timeline">{trackingStages.map((stage,position)=><li key={stage.key} className={exceptional?(position===0?'complete':'pending'):position<index?'complete':position===index?'current':'pending'}><span aria-hidden="true">{position<index&&!exceptional?'✓':position+1}</span><div><strong>{stage.label}</strong>{position===index&&!exceptional&&<small>{current.label}</small>}</div></li>)}</ol>}
