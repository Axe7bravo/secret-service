import { Link } from 'react-router-dom';import type { CustomerOperation } from '../types/customer';import { getCustomerStatus } from '../utils/status';import { CustomerStatusBadge } from './CustomerStatusBadge';
export function CustomerOperationCard({operation}:{operation:CustomerOperation}){const view=getCustomerStatus(operation.status);return <article className="operation-card"><div className="operation-card__top"><div><span className="reference">{operation.operationId}</span><h3>{operation.packageName}</h3></div><CustomerStatusBadge status={operation.status}/></div><p>{view.description}</p><dl><div><dt>Recipient</dt><dd>{operation.recipient.name}</dd></div><div><dt>Requested delivery</dt><dd>{operation.delivery.requestedDate}</dd></div><div><dt>Amount</dt><dd>R {operation.amount.toFixed(2)}</dd></div></dl><Link to={`/operations/${operation.operationId}`}>View operation →</Link></article>}
