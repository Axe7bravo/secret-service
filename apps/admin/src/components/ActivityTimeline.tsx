import type { OperationActivity } from '../types/operations';import { OperationStatusBadge } from './OperationStatusBadge';
export function ActivityTimeline({activity}:{activity:readonly OperationActivity[]}){return <ol className="timeline">{[...activity].reverse().map(item=><li key={item.id}><div><OperationStatusBadge status={item.toStatus}/><time>{new Date(item.timestamp).toLocaleString('en-ZA')}</time></div><p>{item.fromStatus?`${item.actor} moved this operation from ${item.fromStatus.replaceAll('_',' ')}.`:`${item.actor} created this entry.`}</p>{item.note&&<blockquote>{item.note}</blockquote>}</li>)}</ol>}
