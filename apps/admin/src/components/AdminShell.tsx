import { useState } from 'react';import { NavLink,Outlet,useNavigate } from 'react-router-dom';import { useAdminAuth } from '../auth/adminAuthContext';
export function AdminShell(){const [open,setOpen]=useState(false);const navigate=useNavigate();const {user,signOut}=useAdminAuth();const logout=async()=>{await signOut();navigate('/login',{replace:true})};return <div className="admin-layout"><button className={`backdrop ${open?'show':''}`} aria-label="Close navigation" onClick={()=>setOpen(false)}/><aside className={open?'open':''}><div className="brand"><b>S</b><span><strong>SECRET SERVICE</strong><small>ADMIN OPERATIONS</small></span></div><nav><NavLink to="/dashboard" onClick={()=>setOpen(false)}>Dashboard</NavLink><NavLink to="/operations" onClick={()=>setOpen(false)}>Operations</NavLink></nav><div className="profile"><strong>{user?.email??'Admin User'}</strong><span>Operations Control</span><button onClick={()=>void logout()}>Sign out</button></div></aside><div className="workspace"><header className="topbar"><button aria-label="Open navigation" onClick={()=>setOpen(true)}>☰</button><strong>Operations Control</strong><span>● SYSTEM ONLINE</span></header><Outlet/></div></div>}
