import { Link } from 'react-router-dom';import type { Operation } from '../types/operations';import { OperationStatusBadge } from './OperationStatusBadge';
export function OperationList({operations}:{operations:readonly Operation[]}){return <div className="table-scroll"><table className="operations-table"><thead><tr><th>Operation</th><th>Customer</th><th>Package</th><th>Campus</th><th>Status</th><th>Created</th></tr></thead><tbody>{operations.map(item=><tr key={item.operationId}><td><Link to={`/operations/${item.operationId}`}>{item.operationId}</Link></td><td>{item.customerName}</td><td>{item.packageType}</td><td>{item.campus}</td><td><OperationStatusBadge status={item.operationStatus}/></td><td>{new Date(item.createdAt).toLocaleDateString('en-ZA')}</td></tr>)}</tbody></table></div>}
