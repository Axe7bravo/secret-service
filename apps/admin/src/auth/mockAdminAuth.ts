/**
 * DEVELOPMENT PROTOTYPE ONLY.
 * This module and its credentials MUST be removed when Firebase Authentication is introduced.
 * It provides no security and must never be treated as production authorization.
 */
export const MOCK_ADMIN_CREDENTIALS = {
  email: 'admin@secretservice.co.za',
  password: 'Operation2026!',
} as const;

export const MOCK_ADMIN_SESSION_KEY = 'secret-service-admin-mock-session';
